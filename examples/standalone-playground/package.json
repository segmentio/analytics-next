{
  "name": "standalone-playground",
  "private": true,
  "installConfig": {
    "hoistingLimits": "workspaces"
  },
  "scripts": {
    "watch": "NODE_ENV=production yarn run -T browser umd --watch",
    "start": "yarn concurrently 'yarn watch' 'npx http-server .'",
    "dev": "yarn start"
  },
  "dependencies": {
    "@segment/analytics-next": "workspace:^"
  },
  "devDependencies": {
    "concurrently": "^7.2.1"
  }
}
