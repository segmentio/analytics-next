# analytics-next — Twilio Buildkite pipeline, CDN release only.
#
# Deliberately narrow scope: this does NOT cover CI/test (moved to GitHub
# Actions — see .github/workflows/ci.yml) or npm publish (also GitHub
# Actions, release.yml — npm's OIDC trusted publishing only trusts GitHub
# Actions as an issuer, Buildkite isn't an option there). This pipeline
# exists solely to push built browser bundles to the CDN's S3 buckets,
# which live in Segment's own AWS account — internal policy requires that
# kind of infra write to run on Buildkite, not GitHub Actions (see
# docs/scm/twilio-internal/guides/github-actions-acceptable-use.md: "Deploy
# code / infra ... Never GHA").
#
# Auth mirrors the pattern confirmed working on segmentio/ajs-renderer's
# Twilio Buildkite migration (same queue, same cross-account shape into the
# same Segment AWS account, 528451384384): SEGMENT_CONTEXTS: aws-credentials
# gives the agent an ambient identity in Twilio's own account; that identity
# then assumes a role that lives IN Segment's account via a plain
# `aws sts assume-role` call. No OIDC federation, and no dependency on
# segmentio/buildkite-system's cloned hooks/lib (SEGMENT_LIB_PATH) — that
# convention doesn't exist on Twilio agents, which is why the old
# release.sh (run-with-role) never worked here.
#
# NOT YET CONFIRMED — validate before relying on this in production:
#   * That AJS_PRIVATE_ASSETS_UPLOAD's trust policy (in Segment's account)
#     actually permits assumption from this same ambient Twilio identity.
#     ajs-renderer's `buildkite-agent` role does; this one hasn't been
#     checked and may need a trust-policy update on Segment's side.
#   * The actual secret name/contents below (`analytics-next-cdn-release`)
#     is a guess at the convention, not a confirmed-provisioned name. It
#     needs to exist as a Buildkite cluster secret whose value is a
#     dotenv-style blob with these keys (values from wherever they were
#     chamber-stored before, e.g. `buildkite-analytics-next`):
#       AJS_PRIVATE_ASSETS_UPLOAD (an IAM role ARN in account 528451384384)
#       PROD_BUCKET / STAGE_BUCKET
#       PROD_SHADOW / STAGE_SHADOW
#       PROD_CDN_OAI / STAGE_CDN_OAI
#       PROD_CUSTOM_DOMAIN_OAI / STAGE_CUSTOM_DOMAIN_OAI
#   * This pipeline object + its GitHub webhook still need to be created in
#     Buildkite and connected to this repo — this file alone doesn't do that.

agents:
  queue: general-039

env:
  SEGMENT_CONTEXTS: 'aws-credentials'
  # Twilio's own ECR mirror — same account as the agent's ambient identity,
  # so no cross-account login needed to pull it (unlike the Segment-account
  # role assumption below, which is only for the CDN upload step itself).
  NODE_IMAGE: '018537234677.dkr.ecr.us-east-1.amazonaws.com/docker.io/library/node:20.19.4-bookworm'

steps:
  - block: Allow this build to run?
    prompt: ':rotating_light: Review the PR for concerning changes before unblocking'
    if: build.pull_request.repository.fork
    blocked_state: running

  - label: '[Browser] Release to CDN :rocket:'
    # Same release gate as npm publish (release.yml on GitHub Actions) —
    # only fires on the "Version Packages" merge, not on every branch push.
    if: |
      build.branch == "master" &&
      build.message =~ /^Version Packages/ &&
      build.message !~ /\[skip\]/i
    commands: |
      echo "--- Fetch CDN release config"
      eval "$(buildkite-agent secret get analytics-next-cdn-release --format env)"

      echo "--- Assume Segment CDN upload role"
      CREDS=$(aws sts assume-role \
        --role-arn "$AJS_PRIVATE_ASSETS_UPLOAD" \
        --role-session-name "buildkite-$BUILDKITE_PIPELINE_SLUG-$BUILDKITE_BUILD_NUMBER-cdn" \
        --query 'Credentials' --output json)
      export AWS_ACCESS_KEY_ID=$(echo "$CREDS" | jq -r .AccessKeyId)
      export AWS_SECRET_ACCESS_KEY=$(echo "$CREDS" | jq -r .SecretAccessKey)
      export AWS_SESSION_TOKEN=$(echo "$CREDS" | jq -r .SessionToken)

      echo "+++ Release to CDN"
      docker run --rm \
        -v "$$PWD:/workdir" -w /workdir \
        -e AWS_ACCESS_KEY_ID -e AWS_SECRET_ACCESS_KEY -e AWS_SESSION_TOKEN \
        -e PROD_BUCKET -e STAGE_BUCKET -e PROD_SHADOW -e STAGE_SHADOW \
        -e PROD_CDN_OAI -e STAGE_CDN_OAI -e PROD_CUSTOM_DOMAIN_OAI -e STAGE_CUSTOM_DOMAIN_OAI \
        ${NODE_IMAGE} \
        sh -c '
          echo "--- Install dependencies"
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 HUSKY=0 yarn install --immutable
          echo "+++ Build and release to CDN"
          yarn run -T browser release:cdn
        '
